overwrite: true
schema: ./graphql/schemas/**/*.graphql
generates:
  ./graphql/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
  ./graphql/resolvers-types.d.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers